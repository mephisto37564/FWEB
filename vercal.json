{
  "buildCommand": "cd project-2 && npm run build",
  "installCommand": "npm install && cd project-2 && npm install && cd ../backend && npm install",
  "outputDirectory": "project-2/dist",
  "env": {
    "VITE_API_URL": "@vite_api_url"
  }
}